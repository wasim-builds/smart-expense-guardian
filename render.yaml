services:
  - type: web
    name: smart-expense-guardian-api
    env: docker
    plan: free
    dockerContext: .
    dockerfilePath: backend/Dockerfile
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: smart-expense-guardian-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: smart-expense-guardian-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: smart-expense-guardian-redis
          property: connectionString
      - key: CELERY_RESULT_BACKEND
        fromService:
          type: redis
          name: smart-expense-guardian-redis
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: CORS_ORIGINS
        value: "https://smart-expense-guardian.vercel.app"

  - type: redis
    name: smart-expense-guardian-redis
    plan: free
    ipAllowList: [] # only allow internal connections

databases:
  - name: smart-expense-guardian-db
    databaseName: smart_expense
    user: seg_user
    plan: free
